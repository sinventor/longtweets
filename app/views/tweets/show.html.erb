<div class="tweet-display">
  <p id="notice"><%= notice %></p>
  <% if @tweet.deliver %>
    <p>Твит был опубликован в сети от вашего имени!</p>
  <% else %>
    <p>Задача для отправки твита в обработке!</p>
  <% end %>
  <p class="p__tweet-text">
    <%= @tweet.text %>
  </p>

  <%= link_to 'Edit', edit_tweet_path(@tweet) %> |
  <%= link_to 'Back', tweets_path %>
</div>
